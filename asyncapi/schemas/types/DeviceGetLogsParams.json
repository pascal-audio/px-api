{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Parameters for logs_get\n\n# Fields * `limit` - Maximum number of log entries to return * `offset` - Number of entries to skip (for pagination)",
  "properties": {
    "limit": {
      "description": "Maximum number of log entries to return (default: all)",
      "format": "uint",
      "minimum": 0.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "offset": {
      "description": "Number of entries to skip from the beginning",
      "format": "uint",
      "minimum": 0.0,
      "type": [
        "integer",
        "null"
      ]
    }
  },
  "title": "DeviceGetLogsParams",
  "type": "object"
}