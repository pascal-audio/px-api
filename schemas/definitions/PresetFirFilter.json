{
  "description": "FIR (Finite Impulse Response) filter for preset processing layer.\n\n# Internal Type Used in **preset processing layer** from factory presets. Part of speaker model voicing. Path: `/audio/output/speaker/1/preset/fir`\n\n# Purpose Factory-tuned FIR filters that define speaker's frequency response. Contains: - Driver voicing curves - Phase linearization - Cabinet compensation\n\n# Structure - `bypass`: Enable/disable FIR filter (usually not bypassed in factory presets) - `taps`: FIR coefficient array (float32, max 2048 taps)\n\n# Preset Locking When preset is vendor-locked (encrypted), FIR taps are hidden from API responses. Only bypass state is visible/modifiable.",
  "type": "object",
  "required": [
    "bypass",
    "taps"
  ],
  "properties": {
    "bypass": {
      "description": "Enable/disable FIR filter",
      "type": "boolean"
    },
    "taps": {
      "description": "Factory FIR filter coefficients (max 2048 taps, may be locked/hidden)",
      "allOf": [
        {
          "$ref": "./PresetFirTaps.json"
        }
      ]
    }
  }
}